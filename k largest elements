lass Solution {
    int[] kLargest(int[] arr, int n, int k) {
        Arrays.sort(arr);
        int j=n-1;
        int[] ans=new int[k];
        int i=0;
        while(i<k){
            ans[i]=arr[j];
            i++;
            j--;
        }
        return ans;
      
    }
}
